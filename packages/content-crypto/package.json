{
  "name": "@unfiled/content-crypto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Versioned envelope encryption for Unfiled content.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  }
}
