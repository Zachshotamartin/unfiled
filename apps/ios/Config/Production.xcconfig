#include "Base.xcconfig"

UNFILED_APP_BUNDLE_IDENTIFIER = com.zachshotamartin.unfiled
UNFILED_DISPLAY_NAME = Unfiled
UNFILED_API_BASE_URL = https:/$()/unfiled-web.vercel.app/api/v1
UNFILED_MANAGED_AI_FALLBACK_AVAILABLE = NO
